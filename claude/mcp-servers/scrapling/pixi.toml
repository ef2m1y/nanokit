[workspace]
name = "scrapling-mcp"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[dependencies]
python = ">=3.10,<3.14"

[pypi-dependencies]
scrapling = { version = "*", extras = ["ai"] }

[tasks]
mcp = "scrapling mcp"
install-browsers = "python -m playwright install chromium"
